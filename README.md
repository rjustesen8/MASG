# Midwest Athletic Strategy Group Website — V5 Three-Page Version

This version splits the site into three balanced pages:

- `index.html` — cinematic home page with hero video, challenge, plan previews, and sample money visual.
- `plans.html` — plan details, pricing formula, estimator, process, data used, FAQ, and contact form.
- `sample-report.html` — sample report proof page with disclaimer, report preview, sample visuals, and CTA.

Before publishing:
1. Replace `YOURDOMAIN.com` in HTML, robots.txt, and sitemap.xml.
2. Replace `YOUR_FORM_ID` in `plans.html` with a real Formspree ID or connect another form provider.
3. Compress hero videos for faster page load if needed.


V12 update: compacted the fixed top navigation banner and reduced the logo footprint while keeping the same site design.
